# Portfolio Evidence Inventory — Phase 1 Audit

Date verified: 2026-07-26
Scope: `crvro-site` (this repository only). This repo is the **portfolio site**; it does not contain the source code for Sidecar, OpenClaw/Hermes, GroundRules, or Parcel Engine. Those are separate systems described in `src/data/projects.js` copy and illustrated with screenshots in `public/images/`.

## Headline finding — read before Phase 2/3

**This repository has zero test infrastructure.** There is no test runner in `package.json` (`grep -n test package.json` returns nothing), no `vitest`/`jest`/`playwright`/`cypress` dependency, and no `*.test.*` / `*.spec.*` file anywhere in the tree. Only Parcel Engine has an external repo link (`https://github.com/chrisrivero-dev/parcel-engine`); Sidecar, OpenClaw/Hermes, and GroundRules have `repo: null` in the data file and no local checkout exists under this working directory.

Consequence: **no quantitative metric (test count, pass rate, latency, confidence score, closure tolerance, etc.) can currently be sourced or reproduced from anything in this repository.** Per the repository rules ("any displayed metric must be traced to an existing test artifact or generated by a reproducible test command"), I cannot populate the metric table below with real numbers — there aren't any to find here.

**Stopping point:** before Phase 3 (evidence panels) can show anything numeric, one of these needs to happen:
1. You point me at the actual project repos/artifacts (e.g., clone `parcel-engine` locally, or share sanitized test output/logs from Sidecar, Hermes, GroundRules) so I can extract real, reproducible figures, or
2. Evidence panels ship **qualitative only** — the input → stages → output → verification-state workflow text already present in `projects.js`, plus existing sanitized screenshots — with no numbers at all. This is fully compliant with the no-invention rule and is what I'd recommend absent (1).

Everything below is the audit; the metric table is intentionally empty of invented values.

---

## Per-project inventory

### 1. Sidecar (`src/data/projects.js:10-151`)

- **Real input**: incoming Freshdesk ticket message (agent-facing ticket form, `sidecar-ticket-form.png`).
- **Processing stages**: Tampermonkey reads ticket context → `/generate_reply` searches `kb.json` → grounded draft or safe fallback generated → confidence/risk context computed → draft inserted into Freshdesk reply box.
- **Real output**: draft reply inserted into the Freshdesk reply box (`sidecar-draft-response.png`).
- **Safety/verification state**: safe-fallback path when no KB match exists (asks for more info instead of guessing); manual agent approval before send; separately, the Telegram KB-update path requires `/kb_approve <draft_id>` before `kb.json` changes go live.
- **Existing reproducible tests**: none found in this repo. `stack.Testing` in the data file lists "Freshdesk test tickets," "curl / manual API tests," "End-to-end KB deploy verification" — these are described as manual/informal, not automated, and no artifacts (logs, scripts) are present here to reproduce or cite.
- **Unverifiable claims currently in copy**: none of the Sidecar copy states a number, so nothing to flag as a fabricated metric — but also nothing to cite as evidence beyond the screenshots.
- **Sanitized artifacts available for public display**: `sidecar-ticket-form.png`, `sidecar-kb-card.png`, `sidecar-telegram-kb-admin-bot.png`, `sidecar-draft-response.png`, `sidecar-confidence-risk.png` (all in `public/images/`, already used in the case study).
- **Data-provenance disclosure needed (Phase 5)**: copy does not currently state whether the screenshots use real ticket content, synthetic tickets, or sanitized/redacted real tickets. **I could not find any repository documentation (README, comments, case-study copy) that answers this.** Do not guess — confirm with you before writing a disclosure line.
- **Attribution note (Phase 5)**: copy already correctly frames OpenClaw agent (not Hermes) as the KB-drafting agent inside Sidecar's Telegram loop; worth double-checking this doesn't conflate with the separate OpenClaw/Hermes project once that disclosure is rewritten.

### 2. OpenClaw / Hermes (`src/data/projects.js:156-282`)

- **Real input**: short Telegram message (e.g. "remind me Tuesday morning").
- **Processing stages**: Telegram front-end → Hermes local gateway brokers input/inference/tool execution → routing layer classifies intent and extracts tool arguments → typed tool schema validates arguments → dry-run preview generated.
- **Real output**: confirmed tool execution (calendar entry, file move, note append, summary) recorded to an append-only audit log.
- **Safety/verification state**: confirmation gate before any side-effecting action runs; typed schemas reject bad/incomplete arguments before execution; audit log records command, tool, proposed action, confirmation state, and result.
- **Existing reproducible tests**: none found. `next` list item explicitly says "Improve routing tests for common command patterns" — i.e., the project's own roadmap admits routing tests don't yet exist (or aren't solid), which is a useful, honest data point rather than a metric.
- **Unverifiable claims**: none numeric in current copy.
- **Sanitized artifacts available**: `openclaw-telegram-01-reminder.png`, `openclaw-telegram-02-notes-summary.png`, `openclaw-telegram-01-Followup.png`, `openclaw-telegram-04-work-log.png`, `openclaw-hermes-skills-learning-diagram.png`. Note: the "Audit log" screenshot entry (`screenshots[2]`) uses `mockup: 'audit-log'` — i.e., it is **already a mockup, not a real audit-log screenshot**. Must stay clearly labeled as illustrative in any evidence panel; do not present it as a captured artifact.
- **Attribution requirement (Phase 5)**: copy already says "It runs on top of Hermes — a local gateway/control environment," which is reasonably attributed, but the project title itself is "OpenClaw / Hermes" and case-study prose alternates between "OpenClaw" (the assistant) and "Hermes" (the gateway) in a way that could read as one thing Christopher built end-to-end. Needs a clear line separating: Hermes Agent (open-source foundation, not authored by Christopher) vs. OpenClaw (the routing/tool layer, Telegram front-end, typed tool schemas, audit log — built by Christopher on top of it). I don't have the Hermes Agent upstream project's actual name/license in front of me — confirm the correct upstream name before publishing wording.

### 3. GroundRules (`src/data/projects.js:567-656`)

- **Real input**: address or parcel entered by the user.
- **Processing stages**: address/parcel lookup → public-record data extraction and normalization (parcel, zoning, jurisdiction, hazard sources) → plain-English feasibility report generation with source framing.
- **Real output**: cited, reviewable first-pass property screening report (categorized into possible / risky / unknown / needs-verification).
- **Safety/verification state**: "unsupported-jurisdiction messaging" — guardrail that labels unsupported areas instead of overclaiming; "what needs human or official verification" is a first-class report category.
- **Existing reproducible tests**: none in this repo (no GroundRules source present at all; it's a live product at `usegroundrules.com` referenced only as `demo`).
- **Unverifiable claims**: none numeric currently in copy.
- **Sanitized artifacts available**: `groundrules-preview1.png` only (single screenshot).
- **Live demo**: `https://www.usegroundrules.com/` is a real, reachable product — this is itself a strong piece of evidence (a working system, not a mockup) and should be leaned on in the evidence panel rather than fabricated numbers.

### 4. Parcel Engine (`src/data/projects.js:663-743`, GIS project)

- **Real input**: metes-and-bounds legal description text (quadrant, compact, or cardinal bearing formats).
- **Processing stages**: structured parser extracts line/curve calls → bearing/distance parsing → COGO coordinate geometry builder → closure/misclosure validation.
- **Real output**: DXF or GeoJSON export, plus desktop preview geometry.
- **Safety/verification state**: closure/misclosure validation is explicitly the verification gate ("the most reliable feedback signal for whether a description was interpreted correctly" per `learned`); disclaimer already present in data: "It does not include official County data, real APNs, private legal descriptions, Q-drive files, or production records."
- **Existing reproducible tests**: **this is the one project with a real external repo** (`https://github.com/chrisrivero-dev/parcel-engine`), which I have not cloned or inspected in this session — it lives outside `/Users/chrisrivero/Projects/site`. If it has a test suite, cloning it and running its tests would be the one legitimate source of a real, reproducible number for this portfolio (e.g., "N unit tests passing" or "closure tolerance = X"). I have not done this yet — say the word and I will clone it read-only and check.
- **Sanitized artifacts available**: `parcel-enging.png`, `parcel-engine-desktop-preview.png`.

---

## Proposed public metrics — table (currently empty)

| Metric label | Exact value | Source file / command | Reproduction instructions | Date verified | Safe to publish |
|---|---|---|---|---|---|
| *(none yet)* | — | — | — | — | **No** — no reproducible source found in this repo for any of the four projects. Do not publish a number here until one of the two options in "Headline finding" is resolved. |

---

## Phase 2 findings (repairs), surfaced early since they were visible during the audit

These are concrete, low-risk bugs found while reading the code — flagging now so you can decide before I touch anything:

1. **Duplicated wording bug** — [src/data/projects.js:147](src/data/projects.js#L147): `'Add decision log support for management-approved wording-approved wording so team leads can flag canonical answers.'` — the phrase "wording-approved wording" is a duplication artifact. Likely intended: "Add decision log support for management-approved wording so team leads can flag canonical answers."
2. **Email inconsistency** — [src/components/Contact.jsx:19-20](src/components/Contact.jsx#L19): visible text reads `contact@crvro.com` but the `mailto:` target is `christopherarivero@gmail.com`. [src/components/Footer.jsx:12](src/components/Footer.jsx#L12) uses `contact@crvro.com` for both display and `mailto:` consistently. The [README.md:117-125](README.md#L117) (last touched under earlier commits, may be stale) states forwarding for `contact@crvro.com` was **not yet configured** as of that writing and mail would bounce. **I need you to confirm current status** — is `contact@crvro.com` forwarding live now? That determines which address/behavior is correct to standardize on.
3. **Missing `/robots.txt` and `/sitemap.xml`** — neither exists under `public/`.
4. **No per-route document title/meta** — `App.jsx` swaps in `<CaseStudy>` on `/projects/:slug` but never updates `document.title` or the meta description; every project page keeps the homepage's static `<title>` from `index.html`.
5. **README drift** — [README.md](README.md) routing list omits `/projects/parcel-engine` and `/projects/groundrules`; file-structure section is missing `CaseStudy.jsx`, `BuilderLog.jsx`, `BuilderTelemetry.jsx`, `HeroSystemTrace.jsx`, `ParcelPreview.jsx`, `GISProjects.jsx`, `ProjectMark.jsx`, `src/data/`, and `src/styles/case-study.css`. Not user-facing, but worth a pass in Phase 2 cleanup.
6. **Routing config looks structurally sound** on inspection — `vercel.json` rewrite, `netlify.toml` redirect, and `public/_redirects` all fall back to `index.html` for non-asset paths, and the hand-rolled router in `App.jsx` matches `/projects/<slug>` against both `PROJECTS` and `GIS_PROJECTS`. I have not yet run a production build + direct-navigation test to confirm this behaves correctly end-to-end (planned for Phase 2/6).

---

## What I have not yet done (waiting on this report)

- Not cloned or inspected the external `parcel-engine` repo.
- Not modified any file.
- Not run `npm run build` or any verification yet — holding until you've seen this inventory, per the instruction to stop before implementing visuals if evidence is missing.
